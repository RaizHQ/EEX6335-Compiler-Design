class MyClass {
    public attribute x : integer;
};

func main() => void {
    local a : integer;
    local b : float;
    
    a := 10;
    b := 5.5;
    
    if (a > 5) then {
        write(a);
    } else {
        write(0);
    };

    while (a > 0) {
        a := a - 1;
    };
}